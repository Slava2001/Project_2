{
    "properties": {
        "rect": {
            "$ref": "./defines/rect.json",
            "description": "Widget bounders"
        },
        "debug": {
            "type": "boolean",
            "default": false,
            "description": "Enable debug mode"
        },
        "id": {
            "type": "string",
            "description": "Widget identifier"
        },
        "is_visible": {
            "type": "boolean",
            "default": true,
            "description": "Is widget visible flag"
        },
        "anchor": {
            "$ref": "./defines/vec2.json",
            "description": "Is anchor offset, if this parameter is set, the widget position will be calculated relative to the parent size."
        }
    }
}
